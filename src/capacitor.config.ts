import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.be220',
  appName: 'be220',
  webDir: 'www/browser',
  server: {
    androidScheme: 'https'
  }
};

export default config;
