export class Program {
    name: string;
    started: boolean;
    img: string;

    constructor(name: string, started: boolean, img: string) {
        this.name = name;
        this.started = started;
        this.img = img;
    }
}
